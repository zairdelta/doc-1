<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{HH:mm:ss.SSS}){faint} %coloredLevel %clr([%thread]){faint} %clr(%logger{36}){cyan} - %msg%n"/>

    <!-- Custom color mapping -->
    <conversionRule conversionWord="coloredLevel"
                    converterClass="ch.qos.logback.classic.pattern.ColorConverter"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %highlight(
                %d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n
                ){FATAL=red blink, ERROR=red, WARN=yellow, INFO=green, DEBUG=blue, TRACE=cyan}
            </pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
